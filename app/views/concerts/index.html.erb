<div class="container" >
  <h1>Concerts</h1>
  <div class="row multi-columns-row">
    <% @concerts.each do |concert| %>
      <div class="col-sm-6 col-md-3 col-lg-3">
        <div class="card"  style="">
          <ul class="text-center p-1">
            <li class="list-unstyled"><%= link_to concert.title, concert_path(concert) %> </li>
            <li class="list-unstyled"><%= link_to concert_path(concert) do %>
              <%= cl_image_tag concert.photo&.key, height: 100, width: 100, crop: :scale %>
            <% end %>
            </li>
            <li class="list-unstyled"><%= concert.date %> </li>
          </ul>
        </div>
      </div>
    <% end %>
  </div>
</div>
